# Teste — SP_ORG_Items 0.12-C.2

Execute **SP_ORG_Items 0.12-C.2 — Runner Completion + Catalog Arrow Parity 494** duas vezes na mesma sessão.

Esperado: `BASE=488/488`, `C.2=6/6`, `FAIL=0`, `CUMULATIVO=494/494`.

## Smoke manual
1. Abra a interface após o CONFIG_ALL terminar.
2. Confirme `←` no início e `→` no fim de cada linha do Catálogo.
3. Clique no corpo/ícone: deve apenas selecionar.
4. Clique `←`: deve adicionar uma unidade ao Draft/Kit Selecionado.
5. Clique `→`: deve adicionar fisicamente ao Destino de Aplicação atual.
6. Role rapidamente: espere `CATALOG_FOCUSED`, sem FULL por wheel.
7. No Equipment, `X` e Delete continuam removendo imediatamente.
